# If you see "Unknown authentication plugin sha256_password", set MySQL user to native password:
#   ALTER USER 'tms'@'%' IDENTIFIED WITH mysql_native_password BY 'tms'; FLUSH PRIVILEGES;
DATABASE_URL="mysql://user:password@localhost:3306/tms"
# Optional — defaults used if unset (see prisma/seed.ts)
# SEED_SUPER_ADMIN_PASSWORD=SuperAdmin123!
# SEED_TENANT_PASSWORD=Demo123!
# SEED_DEMO_TENANT=false
NODE_ENV=development
PORT=4000
FRONTEND_ORIGIN=http://localhost:5173
JWT_ACCESS_SECRET=change-me-access-min-32-chars-long!!
JWT_REFRESH_SECRET=change-me-refresh-min-32-chars!!
ACCESS_TOKEN_EXPIRES=60m
REFRESH_TOKEN_EXPIRES=7d
COOKIE_DOMAIN=
# Email (optional in dev — invitation logs warning if SMTP missing)
EMAIL_USER=sagarbhavik128@gmail.com
EMAIL_PASS=lzgv pusu ksnv dspx
EMAIL_PORT=465 #TLS
EMAIL_HOST=smtp.gmail.com
FRONTEND_URL=http://localhost:5173
