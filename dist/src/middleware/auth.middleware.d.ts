import type { RequestHandler } from "express";
export declare const authMiddleware: RequestHandler;
export declare const requireTenantUser: RequestHandler;
export declare const requirePlatformUser: RequestHandler;
